# ─── PostgreSQL ───────────────────────────────────────────────────────────────
DB_USER=admin
DB_PASSWORD=your_strong_password
DB_NAME=labor_market
DB_HOST=localhost          # у Docker замінюється на 'db' автоматично
DB_PORT=5432

# ─── pgAdmin (веб-інтерфейс до бази) ─────────────────────────────────────────
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=your_pgadmin_password

# ─── Groq API (LLM для NLP-обробки) ──────────────────────────────────────────
# Безкоштовно: https://console.groq.com → API Keys
GROQ_API_KEY=gsk_REPLACE_WITH_YOUR_KEY

# ─── REST API (CORS для React) ────────────────────────────────────────────────
# Через кому — список дозволених origins. У проді — лише твій домен.
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ─── Адмін-панель / JWT (ОБОВ'ЯЗКОВО змінити у проді!) ────────────────────────
# Без цих змінних код бере дефолти, які видно у публічному репозиторії.
# JWT_SECRET згенерувати: openssl rand -hex 32
JWT_SECRET=REPLACE_WITH_openssl_rand_hex_32
ADMIN_USERNAME=changeme
ADMIN_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
